# Hack2026
Весна 2026

# Backend — Архитектура и ключевые решения

## Стек
- **FastAPI**
- **SQLAlchemy 2.0**
- **PostgreSQL**
- **Alembic**
- **python-docx + matplotlib** - генерация отчётов
- **JWT (PyJWT) + argon2** - аутентификация и хеширование паролей
- **GigaChat API** - генерация текстовых выводов по результатам теста

---
## Структура проекта

```
backend/
├── app/
│   ├── core/
│   │   ├── config.py          # Настройки приложения
│   │   └── image.py           # Утилита сохранения изображений на диск
│   ├── db/
│   │   ├── base.py            # Декларативная база SQLAlchemy
│   │   └── session.py         # Фабрика async-сессий
│   ├── models/                # SQLAlchemy-модели
│   ├── routers/               # FastAPI-роутеры (HTTP-эндпоинты)
│   ├── schemas/               # Pydantic-схемы (валидация запросов/ответов)
│   ├── services/              # Бизнес-логика
│   ├── auth.py                # JWT, хеширование, dependency-функции
│   └── main.py                # Точка входа, регистрация роутеров
├── migrations/                # Alembic-миграции
└── media/                     # Загруженные файлы (фото пользователей, тесты)
```

---
## Архитектура

Приложение построено по **трёхслойной архитектуре** внутри монолита:

```
HTTP-запрос
    ↓
Роутер (routers/)        — принимает запрос, валидирует через схемы, вызывает сервис
    ↓
Сервис (services/)       — бизнес-логика, не знает об HTTP
    ↓
Модель (models/)         — SQLAlchemy ORM, описывает таблицы БД
    ↓
PostgreSQL
```

Каждый слой отвечает только за своё — роутеры не содержат бизнес-логики, сервисы не знают про HTTP-статусы.

---

## Модели и схема БД

### Основные таблицы

| Таблица            | Описание                           |
| ------------------ | ---------------------------------- |
| `users`            | Психологи и администраторы системы |
| `tests`            | Тесты созданные психологами        |
| `sections`         | Разделы теста                      |
| `questions`        | Вопросы теста                      |
| `formulas`         | Формулы для подсчёта метрик        |
| `question_metrics` | M2M связь вопросов и формул        |
| `sessions`         | Сессии прохождения теста гостями   |
| `answers`          | Ответы гостей на вопросы           |

### Ключевые решения по моделям

**JSONB для гибких данных.** Поля `config` (вопросы), `client_extra` (доп. поля гостя), `metrics` (результаты), `report_template` (шаблон отчёта) хранятся как JSONB. Это позволяет не менять схему БД при добавлении новых типов вопросов или полей отчёта.

**Вес ответа сохраняется в момент ответа.** Поле `answers.weight` фиксирует вес в момент прохождения. Если психолог изменит веса вопросов — старые результаты не пересчитываются, история остаётся неизменной.

---

## Сервисный слой

### `services/test_io.py`

Центральный модуль работы с тестами. Содержит:

- **`import_test`** — разбивает большой JSON теста по таблицам БД. Генерирует новые UUID для всех объектов (тест, секции, вопросы, формулы), обновляет `next_question_id` в опциях и ссылки в `report_template` на новые UUID.
- **`export_test`** — собирает полный JSON теста из БД обратно в формат импорта (включая веса).
- **`get_test_for_guest`** — отдаёт тест гостю без весов и `score_ranges`. Все вопросы включая скрытые — фронт управляет видимостью через флаг `hidden_by_default`.
- **`save_session_answers`** — принимает ответы от фронта, вычисляет вес каждого ответа через `resolve_answer_weight`, сохраняет в таблицу `answers`.
- **`calculate_and_save_metrics`** — после сохранения ответов считает все метрики по формулам (`sum`, `avg`, `min`, `max`, `percent`), находит интерпретацию по диапазонам, сохраняет в `session.metrics`.

### `services/report_service.py`

Генерация docx-отчётов на лету без сохранения на диск:

- Два отчёта: для гостя (`report_template.client`) и для психолога (`report_template.psychologist`).
- Шаблон отчёта состоит из блоков четырёх типов: `text`, `metric`, `chart`, `answers`.
- Графики (`bar`, `radar`) рисуются через matplotlib в `BytesIO` и вставляются в docx как изображения.
- Готовый документ отдаётся через `StreamingResponse` — файл никогда не попадает на диск сервера.

### `services/gigachat.py`

Интеграция с GigaChat API для генерации текстового вывода по результатам прохождения теста.

---

## Аутентификация

Двухтокенная схема:

- **Access token** — короткоживущий JWT, передаётся в заголовке `Authorization: Bearer`.
- **Refresh token** — долгоживущий, хранится в БД в таблице `refresh_tokens` в виде SHA-256 хеша. Используется для получения нового access token.

Роли: `admin`, `psychologist`. Доступ к эндпоинтам контролируется через FastAPI dependencies (`get_current_psychologist`, `get_current_active_psychologist`).

Проверка активности аккаунта через `user.has_active_access` — учитывает флаг `is_blocked` и поле `access_until`.

---

## Флоу прохождения теста

```
1. GET  /guest/{access_link}              — получить тест (без весов)
2. POST /guest/{access_link}/start        — создать сессию, получить session_id
3. POST /guest/sessions/{id}/answers      — отправить ответы
                                            → автоматически считаются метрики
4. GET  /guest/sessions/{id}/report       — скачать docx-отчёт
```

---

## Импорт и экспорт тестов

Психолог работает с тестом через единый JSON-формат. При импорте все UUID генерируются заново — каждый импорт создаёт независимую копию теста. Это позволяет делиться шаблонами тестов между психологами без конфликтов.

`next_question_id` в опциях вопросов (ветвление) автоматически обновляется на новые UUID при импорте.

---

# Frontend — Архитектура и ключевые решения

## Стек

- **Nuxt 3** — SSR-фреймворк на базе Vue 3
- **TypeScript**
- **Pinia** — управление состоянием
- **Tailwind CSS** — утилитарные стили
- **vue-draggable-plus** — drag-and-drop в билдере тестов
- **axios / $fetch** — HTTP-запросы к бэкенду

---

## Структура проекта
```
frontend/
├── app/
│   ├── assets/
│   │   ├── css/          # Глобальные стили, CSS-переменные, шрифты
│   │   ├── fonts/        # Шрифты BlenderPro, GothamOffice
│   │   └── images/       # SVG-иконки, иллюстрации
│   ├── components/
│   │   ├── features/test/
│   │   │   ├── MetricItem.vue          # Редактор одной метрики/формулы
│   │   │   ├── QuestionItem.vue        # Редактор одного вопроса
│   │   │   ├── ReportBuilder.vue       # Конструктор шаблона отчёта
│   │   │   ├── ReportTemplateEditor.vue
│   │   │   └── TestBuilder.vue         # Главный компонент билдера теста
│   │   ├── ui/
│   │   │    └── RichTextEditor.vue      # Quill-редактор для поля «О себе»
│   │   └──  QuestionInput.vue        
│   ├── composables/
│   │   └── useApi.ts     # Обёртка над $fetch с авторизацией
│   ├── layouts/
│   │   ├── admin.vue     # Лейаут для панели администратора
│   │   ├── default.vue   # Лейаут психолога (навбар + слот)
│   │   └── empty.vue     # Лейаут для страницы прохождения теста (карточка по центру)
│   ├── middleware/
│   │   ├── auth.ts       # Проверка наличия токена
│   │   └── role.ts       # Проверка роли (admin / psychologist)
│   ├── pages/
│   │   ├── admin/
│   │   │   └── index.vue                          # Список психологов (CRUD)
│   │   ├── psychologist/
│   │   │   ├── index.vue                          # Личный кабинет психолога
│   │   │   └── tests/
│   │   │       ├── index.vue                      # Список тестов
│   │   │       ├── create.vue                     # Создание теста
│   │   │       └── [id]/
│   │   │           ├── edit.vue                   # Редактирование теста
│   │   │           └── submissions/
│   │   │               ├── index.vue              # Список прохождений
│   │   │               └── [submissionId].vue     # Детальный результат
│   │   ├── test/
│   │   │   └── [token].vue   # Публичная страница прохождения теста гостем
│   │   ├── index.vue         # Главная (редирект)
│   │   └── login.vue         # Страница входа
│   └── stores/
│       ├── auth.ts           # Данные пользователя, роль, токен
│       ├── testBuilder.ts    # Состояние билдера теста (секции, вопросы, метрики)
│       └── theme.ts          # Текущая тема (light / dark)
```

---

## Архитектура

### Лейауты

| Лейаут | Используется для |
|--------|-----------------|
| `default` | Все страницы психолога — содержит навбар с навигацией и кнопкой смены темы |
| `admin` | Панель администратора |
| `empty` | Страница прохождения теста — карточка по центру экрана, без навбара |

### Страницы и роутинг

Роутинг файловый (Nuxt 3). Защита маршрутов через два middleware:
```
/login                  — публичная
/test/[token]           — публичная (гость проходит тест)
/psychologist/**        — auth + role (только psychologist и admin)
/admin/**               — auth + role (только admin)
```

---

## Composables

### `useApi.ts`

Центральная обёртка над `$fetch` для всех запросов к бэкенду:

- Автоматически подставляет `baseURL` из `runtimeConfig`
- Добавляет заголовок `Authorization: Bearer <token>` из cookie
- При получении `401` — очищает токен и редиректит на `/login`
```ts
const { api } = useApi()
await api('/users/me', { method: 'PATCH', body: { bio: '...' } })
```

---

## Middleware

### `auth.ts`
Проверяет наличие JWT-токена в cookie. Если токена нет — редиректит на `/login`. Применяется на всех защищённых маршрутах.

### `role.ts`
Проверяет роль пользователя из `authStore`:
- `/admin/**` — только `isAdmin`
- `/psychologist/**` — `isPsychologist` или `isAdmin`

При несоответствии роли — редиректит на подходящую страницу.

---

## Stores

### `auth.ts`
Хранит данные залогиненного пользователя (`name`, `email`, `role`, `access_until`). Предоставляет геттеры `isAdmin` и `isPsychologist`. Используется в middleware и компонентах для условного рендера.

### `testBuilder.ts`
Центральное состояние конструктора тестов. Хранит мета-информацию теста, массив секций с вопросами, метрики и шаблон отчёта. Содержит методы для добавления/удаления секций, вопросов, вариантов ответов, метрик. Метод `buildPayload()` собирает финальный JSON для отправки на бэкенд, `loadFromJson()` — загружает тест из импортированного файла.

### `theme.ts`
Управляет переключением между светлой и тёмной темой. Тема применяется как CSS-класс на `body` (`light-theme` / `dark-theme`). Все цвета вынесены в CSS-переменные — переключение темы не требует изменений в компонентах.

---

## Флоу прохождения теста (гость)
```
1. Психолог копирует ссылку вида /test/{token}
2. GET  /guest/{token}                    — загрузка теста (без весов)
3. Гость заполняет данные о себе (ФИО, email, доп. поля)
4. POST /guest/{token}/start              — создание сессии → session_id
5. Гость отвечает на вопросы
   (видимость управляется на фронте через hidden_by_default и next_question_id)
6. POST /guest/sessions/{id}/answers      — отправка ответов
7. POST /chat/                            — запрос предварительного анализа от ИИ
8. Отображается результат + предварительный анализ GigaChat
```

---

## Темизация

Все цвета проекта определены через CSS-переменные в двух классах: `.light-theme` и `.dark-theme` в `assets/css/globals.css`. Tailwind-классы (`text-green-dark`, `bg-bg-light` и т.д.) ссылаются на эти переменные — смена темы происходит мгновенно без перезагрузки страницы.

---

## Установка и запуск

### Локально
```bash
cd frontend
npm install
npm run dev
```

Приложение запустится на `http://localhost:3000`.

Создай файл `.env` в папке `frontend/`:
```env
NUXT_PUBLIC_API_BASE=http://localhost:8000
```

### Docker

Проект разворачивается через Docker совместно с бэкендом. Конфигурация nginx находится в папке `nginx/`. Переменная окружения `NUXT_PUBLIC_API_BASE` передаётся через `docker-compose.yml`.

## Хранение медиафайлов

Изображения сохраняются локально в директорию `media/` через утилиту `core/image.py`. Поддерживаются форматы JPEG, PNG, WebP. Максимальный размер файла — 2 МБ. Файлу присваивается UUID-имя для избежания коллизий.
